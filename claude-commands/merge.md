Run `merge` to squash-merge the current PR. This performs safety checks first: unresolved review threads and CI state. After merging it switches to the default branch and pulls.

Show the merge result and final branch state.
