Run `review` to pipe the branch diff to claude for an AI code review. If an argument is provided, pass it as the focus: `review "<argument>"` (e.g., `/review security`).

Display the review output and highlight any critical issues.
