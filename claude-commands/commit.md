Stage all changes and commit. If a message is provided as an argument use `commit "<argument>"`, otherwise use `commit --ai` to generate the message from the diff.

Show the result including the commit hash.
