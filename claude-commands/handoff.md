Run `handoff` to build a session handoff block containing current context, recent commits, diff stat, and TODOs. The output is copied to clipboard automatically.

Display the full handoff block and confirm it was copied.
